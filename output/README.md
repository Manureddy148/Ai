This directory holds the submission outputs: `matching_results.tsv` and `candidate_pairs.tsv`.
They are generated by `python code/business_entity_resolution/src/run_pipeline.py --data-dir dataset --out-dir output --work-dir work`.
The TSVs are not committed (too large; see `.gitignore`) - regenerate them with the pipeline or the Kaggle notebook.
Validate with: `python3 utils/validate_submission.py --matching output/matching_results.tsv --candidate output/candidate_pairs.tsv --test-dir dataset/test`
